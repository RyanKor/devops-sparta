name: Homework1

on:
  push:
    branches:
      - test # main 브랜치에서만 작동

jobs:
  homework1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Test Branch Action Runner
        run: |
          echo "Test Branch Action Runner"